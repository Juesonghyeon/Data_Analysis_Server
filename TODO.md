# 개선 필요 항목

## 1. 대학·학과 매칭 품질

현재 Python 모델에서 직업명 문자열을 CSV와 단순 비교해서 대학을 찾는다.
직업명이 조금만 달라도 매칭이 실패하는 구조.

개선 방향:
- 전국대학별학과정보.csv 데이터를 MySQL에 테이블로 이전
- 직업-학과 관계를 별도 관계 테이블로 분리
- 문자열 단순 비교 대신 LIKE 또는 JOIN 쿼리로 조회

---

## 2. 서비스 안정성

### 2-1. DB 비밀번호 평문 노출
application-local.properties에 DB 비밀번호가 평문으로 하드코딩되어 있음.

개선 방향:
- 환경변수로 분리 (예: DB_PASSWORD)
- application.properties에서 ${DB_PASSWORD} 형태로 참조

### 2-2. 프론트 폴백 품질 문제
Python 모델 서버 다운 시 프론트의 로컬 JS 폴백(25개 직업)이 동작하는데,
품질이 너무 낮아 사용자 경험을 해침.

개선 방향:
- 폴백 제거 후 모델 서버 다운 시 사용자에게 명확한 에러 메시지 표시
- 또는 Spring Boot에서 헬스체크 후 503 응답 반환
